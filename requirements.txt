# PII Review — required runtime dependencies
flask>=3.0
waitress>=3.0
python-dotenv>=1.0
PyMuPDF>=1.24
pikepdf>=9.0
pdfplumber>=0.11
Pillow>=10.0
numpy>=1.26

# Presidio for ACCOUNT_NUMBER + structured PII pattern detection
presidio-analyzer>=2.2
spacy>=3.7
# spaCy English models required by the tokenizer. Install separately:
#   python -m spacy download en_core_web_lg

# Optional NER models for the PERSON multi-model consensus.
# Install only the ones you want; the tokenizer silently skips
# unavailable models. See README.md for tradeoffs.
#   pip install transformers torch        # HuggingFace BERT NER
#   pip install flair                     # Flair sequence tagger
#   pip install stanza                    # Stanford Stanza
